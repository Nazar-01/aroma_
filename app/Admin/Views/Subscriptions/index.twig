{% extends "/layout.twig" %}

{% block content %}
<div class="content-wrapper">
  <!-- Content Header (Page header) -->
  <section class="content-header">
    <h1>
      Подписчики
    </h1>
  </section>

  <!-- Main content -->
  <section class="content">

    <!-- Default box -->
    <div class="box">
      <!-- /.box-header -->
      <div class="box-body">
        <div class="form-group">
          <a href="/admin/subs/create" class="btn btn-success">Добавить</a>
        </div>
        <table id="example1" class="table table-bordered table-striped">
          <thead>
            <tr>
              <th>ID</th>
              <th>Email</th>
              <th>Действия</th>
            </tr>
          </thead>
          <tbody>

            {% for sub in subs %}

            <tr>
              <td>{{ sub.id }}</td>
              <td>{{ sub.email }}</td>
              <td>
                <a href="/admin/sub/{{ sub.id }}/edit" class="fa fa-pencil"></a>
                <a href="/admin/sub/{{ sub.id }}/delete" class="fa fa-remove"></a>
              </td>
            </tr>

            {% endfor %}

          </tfoot>
        </table>
      </div>
      <!-- /.box-body -->
    </div>
    <!-- /.box -->

  </section>
  <!-- /.content -->
</div>
{% endblock %}